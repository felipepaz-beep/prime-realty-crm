name: Deploy Edge Functions

on:
  push:
    branches: [main]
    paths:
      - "supabase/functions/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      # evolution-webhook: deploy to BOTH projects so whichever URL the webhook hits gets the latest code
      - name: Deploy evolution-webhook (ghgcnmzrsahfkgotiyuj)
        run: |
          supabase functions deploy evolution-webhook \
            --project-ref ghgcnmzrsahfkgotiyuj \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy evolution-webhook (btotxxumzkusjjiwwtxh)
        run: |
          supabase functions deploy evolution-webhook \
            --project-ref btotxxumzkusjjiwwtxh \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      # google-calendar functions live on the Lovable project (ghgcnmzrsahfkgotiyuj)
      - name: Deploy google-calendar-oauth
        run: |
          supabase functions deploy google-calendar-oauth \
            --project-ref ghgcnmzrsahfkgotiyuj \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy google-calendar-sync
        run: |
          supabase functions deploy google-calendar-sync \
            --project-ref ghgcnmzrsahfkgotiyuj \
            --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
